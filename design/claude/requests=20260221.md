PLAN

the slideshow is a self contained app in one portable repo.  The repo
does not contain the db with the file names nor the folder where the
images reside.  We specify where the db is.  But the db doesn't not know
where the images are without a absolute path.  

I want to change this so that put the path to the images from where the app
is running (absolute or relative) into the params (json, html, etc).
This will then be pre-appended to the path column in the fotos.db

I think changes to the app could be limited to
server.ts:function loadDbImages(params: Params): DbLoadResult 

and the params.  Is that correct?

---------------------------
When I get "Files in fotos.db are not available"  message
1.  if I press esc it works correctly if I try right or left arrow I get a blank totally unresposive screen - just ignore the keystroke.
1.  The error message should include the path/filename it failed to load

----------------------
/Users/mac24/a/projects/fotos/Keep/images/familyHistory/OldPhotos/DorthyRoderick.jpg id=16 should be a portrait mode but appears rotated 90% right.  Is that something that software can correct or do I need to rotate the foto and resave. (its there a builtin imac app to do that?)
